server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/YasiruWickramasinghe/SpringBoot_Microservice_Config_Server
          clone-on-start: true

  # Remove Banner from Console
  main:
    banner-mode: off