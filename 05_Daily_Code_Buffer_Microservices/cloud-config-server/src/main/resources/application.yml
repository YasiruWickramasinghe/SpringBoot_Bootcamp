server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/YasiruWickramasinghe/SpringBoot_Bootcamp
          clone-on-start: true
          searchPaths:
            - 05_Daily_Code_Buffer_Microservices/git-config-server

  # Remove Banner from Console
  main:
    banner-mode: off